source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'VISPER-CommandBus_Example', :exclusive => true do
  pod 'VISPER-CommandBus', :path => '../'
end

target 'VISPER-CommandBus_Tests', :exclusive => true do
  pod 'VISPER-CommandBus', :path => '../'

  
end
